[package]
name = "engine-app"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
engine-core = { path = "../core" }
engine-ecs = { path = "../ecs" }
engine-platform = { path = "../platform" }

winit = { workspace = true }
